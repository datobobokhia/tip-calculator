import { StyledResetButton } from "../../styles/ResetButton.styled";
export default function ResetButton() {
  return (
    <StyledResetButton>
      <button>Reset</button>
    </StyledResetButton>
  );
}
